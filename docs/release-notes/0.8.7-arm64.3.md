# Gas Mask 0.8.7-arm64.3

## 変更点

- About ダイアログの著者表記を「Based on Gas Mask by Siim Raud」に変更
- HostsDataStore の NotificationCenter を DI 可能にし、テストの分離性を向上
- About スクリーンショットテストをヘッドレス環境でも安定動作するよう改善

## 動作環境

- macOS 13 Ventura 以降
- Apple Silicon Mac

## インストール

### 「壊れているため開けません」と表示される場合

本アプリは Apple Developer ID による署名・公証を行っていないため、macOS Gatekeeper によってブロックされることがあります。

**方法 1: ターミナルで quarantine 属性を削除する（推奨）**

```bash
xattr -cr /Applications/Gas\ Mask.app
```

**方法 2: 右クリックから開く**

Finder で `Gas Mask.app` を右クリック（または Control+クリック）→「開く」を選択します。
